package dotj.UI.ImGui.nodes;

import dotj.UI.ImGui.BPGraph;
import dotj.UI.ImGui.BPNode;

public class TestNode extends BPNode {
    public TestNode(BPGraph graph, int id) {
        super(graph, id);
    }
}
